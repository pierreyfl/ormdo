class AddMainToPhotos2 < ActiveRecord::Migration
  def change
    # add_column :photos, :main, :boolean, default: false
    # contacts = Contact.all
    # contacts.each do |contact|
    #   photo = contact.photos.first
    #   photo.main = true
    #   puts photo.save!
    # end
  end
end
