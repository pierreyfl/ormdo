<%= form_for @link_provider, :html => { :class => "form-horizontal link_provider" } do |f| %>

    <% if @link_provider.errors.any? %>
    <div id="error_expl" class="panel panel-danger">
      <div class="panel-heading">
        <h3 class="panel-title"><%= pluralize(@link_provider.errors.count, "error") %> prohibited this link_provider from being saved:</h3>
      </div>
      <div class="panel-body">
        <ul>
        <% @link_provider.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
        </ul>
      </div>
    </div>
  <% end %>

  <div class="control-group">
    <%= f.label :name, :class => 'control-label' %>
    <div class="controls">
      <%= f.text_field :name, :class => 'form-control' %>
    </div>
    <%= error_span(@link_provider[:name]) %>
  </div>


  <%= f.submit nil, :class => 'btn btn-primary' %>
  <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
            link_providers_path, :class => 'btn btn-default' %>

<% end %>
